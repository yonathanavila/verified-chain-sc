{
  "name": "hardhat-project",
  "license": "MIT",
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "dotenv": "^16.0.3",
    "hardhat": "^2.13.0"
  },
  "scripts": {
    "compile": "npx hardhat clean && npx hardhat compile",
    "deploy:scroll": "npx hardhat run --network scrollAlpha scripts/deploy.ts",
    "deploy:testnet": "npx hardhat run --network base-local scripts/deploy.ts",
    "verify": "npx hardhat run scripts/verify.ts --network scrollAlpha"
  },
  "devDependencies": {
    "@types/node": "^18.14.4",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}